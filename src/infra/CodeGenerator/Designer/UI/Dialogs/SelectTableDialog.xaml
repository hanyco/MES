<Window x:Class="CodeGenerator.Designer.UI.Dialogs.SelectTableDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CodeGenerator.Designer.UI.Dialogs"
        mc:Ignorable="d"
        Owner="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
        ResizeMode="NoResize"
        Title="SelectTableDialog" Height="800" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" ToolTip="PromptBar"/>
            <RowDefinition ToolTip="Content"/>
            <RowDefinition Height="Auto" ToolTip="ButtonBar"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Please select a table from the list below:" Margin="10" />
        <TreeView x:Name="TableTreeView"  Grid.Row="1"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Click="OkButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
